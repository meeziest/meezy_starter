import 'package:flutter/material.dart';

class TMButton extends StatelessWidget {
  const TMButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
