import '../../tooltip.dart';

class PathBuilders {
  PathBuilders._();

  static const PathBuilder circleIn = PathBuilderCircle(true);
  static const PathBuilder circleOut = PathBuilderCircle(false);
}
